doctype
html
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Chennai CSV Uploader
  body
    form(method="POST", action="/upload", enctype="multipart/form-data")
      input(type="file", name="document")
      input(type="submit")
